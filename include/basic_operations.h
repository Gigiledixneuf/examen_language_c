#ifndef OPERATIONS_H
#define OPERATIONS_H

#include <stdbool.h>

bool add(
    double a,
    double b,
    double *result
);

bool subtract(
    double a,
    double b,
    double *result
);

bool multiply(
    double a,
    double b,
    double *result
);

bool divide(
    double a,
    double b,
    double *result
);

#endif