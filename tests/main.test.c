#include <stdio.h>
//appel du header du module

int main(void)
{
    // demande d'entrer des input
    //appel de la fonctions
    //capture d'erreur pour ceux qui en renvoi et utilisation
   printf("c'est ici que seront testé chaque fonction");

  return 0;
}